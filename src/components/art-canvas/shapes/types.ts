export type TShapeOptions = {
  bgColor?: string;
  brushWidth: number;
  brushColor: string;
  x?: number;
  y?: number;
  isFilled: boolean;
  startCoords?: TCoords;
};

export type TCoords = {
  x: number;
  y: number;
};
